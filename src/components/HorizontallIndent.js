import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const HorizontallIndent = ({width}) => {
  return (
    <View style={{width: width}}>
    </View>
  )
}

export default HorizontallIndent

const styles = StyleSheet.create({})