import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VerticalIndent = ({height}) => {
  return (
    <View style={{height: height}}>
    </View>
  )
}

export default VerticalIndent

const styles = StyleSheet.create({})